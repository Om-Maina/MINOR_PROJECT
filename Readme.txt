Welcome user,

To open the application you can proceed in two ways

#First way:-
Open the browser and paste the url
"https://gossipbuddy.herokuapp.com/"
to visit our web site.

#Second way:-
To run the program open folder in ide,
and run commands 

#Install Node On Your System
1)Install node
2)Install npm Or Install yarn (command npm i--global yarn)
3)Install nodemon (command npm install --save-dev nodemon)

#To Run application in local host
npm run dev
Now open ( localhost:5000 ) to see our running application. 